import 'package:flutter/material.dart';

class MoreGamesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Mais Jogos")),
      body: Center(child: Text("Tela de Mais Jogos")),
    );
  }
}
